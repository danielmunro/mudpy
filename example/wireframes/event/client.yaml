!<u!event>
events:
    input:
        - {method: proxy,
            callback: "login.step"}
    loggedin:
        - {method: proxy,
            callback: "_loggedin"}
    input.__unhandled__: 
        - {method: subscriber_execute,
            execute: "user.notify('What was that?')"}
